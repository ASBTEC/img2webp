# compose.yml
services:
  img2webp:
    build: .
    user: "${UID}:${GID}"          # run as the host user
    volumes:
      - ./input:/data/in:ro
      - ./output:/data/out
    command: ["--input-location","/data/in","--output-location","/data/out","--verbose"]
